import 'package:clipboard_sync/app.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    title: 'Clipboard Sync',
    theme: ThemeData(
      primarySwatch: Colors.blue,
    ),
    home: App(),
  ));
}